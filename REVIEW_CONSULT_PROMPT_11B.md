# Relay 11B — closing note: Fable's condition resolved; final ledger confirmation (2026-08-21)

Short round. The 11.2(b) decomposition and the frozen per-cell CIs — the
two computations Fable's 11.1 scoring was conditioned on — are done. Both
parties: confirm your final ledger line for the results post, or amend.

## The computed facts (outcome-blind decomposition first, CIs second)

**(i) Position profile, follow-on aggregate arm, reachability d6/d8:** no
late-trace pileup. Interventions in those cells sit at 19.1–23.7% in the
final fifth of the trace — LESS end-concentrated than the connectives-only
arm in the same cells (24.9–35.9%) and far less than the answer-adjacent
cells (composition-d2 53.5–54.7%, multiplication-d2 ~80%). Median distance
to termination: 435–519 tokens. 63–74% of the interventions fall before
the final answer step. The late-trace mechanistic confound named in
Fable's condition is not present.

**(ii) Frozen per-cell CIs, follow-on aggregate:**
reachability-d6 penalty +0.028, 95% CI [−0.026, +0.083];
reachability-d8 penalty +0.031, 95% CI [−0.024, +0.086].
Both include zero.

Per the condition's own terms (confound absent AND CIs exclude zero →
fallen), the second clause does not hold: the aggregate-flat registration
is recorded as **"strained, not fallen"** with both facts printed beside it.

## Capacity proxy (11.2(a), Sol's naming and metrics)

Available reader-neutral channel capacity proxy, decode-path, aggregate
arm: sites with ≥2 eligible alternatives are 4.6% (instruct) / 4.9% (base)
of candidate sites. Nominal uniform-choice capacity: instruct 1.72
bits/trace mean, 1.0 median, 15% of traces zero; base 0.54 bits/trace
mean, median 0.0, 53% of traces zero. Rates: 2.6 / 1.5 nominal bits per
1,000 generated tokens. Labeled as a proxy for available symbol choices,
not demonstrated end-to-end encoding capacity.

## Asks

**(11B.1)** Fable: state the final ledger line for the follow-on aggregate
registration given (i) and (ii). Sol: confirm your 11.1 ledger lines as
final, or amend in light of the decomposition (the position data is the
first report, descriptive and outcome-blind, per your own 11.2(b) terms).

**(11B.2)** Both: the results post will carry Sol's restrained top-line
summary as the abstract, the full prediction ledger, primary+secondary
reads together, the capacity proxy as a headline alongside the
exposure-redistribution finding, and the methods-lessons unsoftened.
Confirm nothing further is required before Noah publishes the prereg post
and results post together.

**(11B.3)** The bridge arm (11.2(c), as amended by Sol: three arms with
exposure-matched control, full freeze list) requires ~$40–60 and a fresh
design-freeze commit. Confirm it proceeds AFTER publication as labeled
exploratory follow-up, or state a reason it must precede.
