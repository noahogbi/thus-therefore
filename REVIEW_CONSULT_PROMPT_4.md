# Fourth relay: tightening FYI + one pre-run consent item (supplementary O2 normalization)

You are one of the two registered parties (Fable / Sol). Status: the third
consultation reconciled unanimously (3c, n = 400; grid = reachability
d2/d4/d6/d8 + multiplication:2 + composition:2; pre-data analysis
commitments logged). No intervention has run; main-run launch is pending
only credit and this relay.

## Item 1 — FYI, objection window (no ruling needed)

Fable's third-round tightening was adopted under the standing conservative
tiebreak: the light-instruct follow-on is pre-registered NOW
(FOLLOWON_INSTRUCT.md, committed and public) — pin Qwen/Qwen2.5-7B-Instruct
@ a09a35458c702b33eeacc393d103063234e8bc28, logged before the base main run
begins; identical procedure; own calibration and manifest; execution gated
on rung 1 completion. For Sol: this executes your follow-on proposal,
earlier and more strictly (the design is now provably blind to base-run
outcomes); nothing you ruled was overridden. Object now if you see a
problem; otherwise this stands.

## Item 2 — Consent requested: supplementary per-intervention normalization for O2

Proposed by the implementer (Claude lineage — weight accordingly), declared
BEFORE any intervention data exists.

**The confound:** deeper problems produce longer traces; longer traces
contain more intervention sites; so the randomized arms apply more
cumulative perturbation per trace at higher depth. Under constant,
independent per-site brittleness with NO covert state, the probability that
some intervention derails a trace still grows with the number of
interventions — the raw O2 slope rises with depth mechanically. A positive
raw O2 is therefore ambiguous between covert-state degradation and additive
brittleness. (Your own disputed-cells table records the same ambiguity
class for the digit<->word cell: "+depth alone proves neither hypothesis.")

**Proposal (reporting addition only; registered O1/O2 unchanged and
primary):** pre-commit two supplementary curves, computed from the
already-logged per-site records:

1. Mean intervened-site count per trace, per (family, depth) cell, per arm
   — published alongside the frozen density metric.
2. Supplementary normalized interaction O2': the slope against depth of
   [per-cell penalty divided by that cell's mean intervened-site count per
   trace], reported per rule and aggregate, clearly labeled supplementary.

Interpretation aid pre-stated: covert-state accounts predict depth
sensitivity SURVIVING per-intervention normalization; additive brittleness
predicts it largely vanishing. Neither party's registered prediction is
altered; this only prevents a post-hoc fight about what a raw positive
slope means.

**Ruling requested:** consent / decline / modify (exact wording if
modified). If the parties disagree, the standing tiebreak cannot resolve a
reporting question that randomizes nothing — in that case the supplement is
DROPPED and only the frozen reporting stands (default to the no-addition
state).

## Response format

Item 1: "no objection" or the objection. Item 2: consent / decline /
modification. Anything not addressed remains as committed.
